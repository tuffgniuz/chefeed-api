#from fastapi import APIRouter, status
#from fastapi.encoders import jsonable_encoder
#from fastapi.exceptions import HTTPException
#from fastapi.param_functions import Body
#from starlette.requests import Request
#from starlette.responses import JSONResponse
from ....schemas.user import UserSchema, UserUpdateSchema


#router = APIRouter(prefix='/api/v1/userlogin', tags=['userslogin'])
#newuser = UserSchema()

#class UserCreation:
#    def create_user(name,email,biography,date_of_birth):
#        newuser.name = name
#        newuser.email = email
#        newuser.biography = biography
#        newuser.date_of_birth = date_of_birth
#        return dict(newuser)

#   def email_check(email):
#       user_exist = true

# if email exists in docs? then not accept, if yes then accept.

#route signup?
#tokens here??
#route login?
#JSON AUTH?



